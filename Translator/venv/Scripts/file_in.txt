int pow(int a) {
    printf(a*a);
}

int f(a) {
    return(a+b+c);
}

void main(char param)
{
    int y = f(1);
    int code;
    if((y<1) && (y>0)) {
        code = y;
    }
    else {
        code = y%2;
    }
    printf(code);
}