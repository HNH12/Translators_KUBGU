int[] pow(int a, int b) {
    printf(a*a);
}

int f(a,b,c) {
    return(a+b+c);
}

void main(char param)
{
    int y = f(1,2,3);
    int code;
    if((y<1) && (y>0)) {
        code = y;
    }
    else {
        code = y%2;
    }
    printf(code);
}